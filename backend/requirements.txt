# backend/requirements.txt
#
# Python dependencies for the FastAPI backend.
# These are installed inside the Docker container during image build.

fastapi
uvicorn[standard]
pydantic
pydantic-settings
neo4j
python-dotenv
google-generativeai
